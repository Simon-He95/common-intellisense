import ActionSheet from './ActionSheet.json'
import Backtop from './Backtop.json'
import Badge from './Badge.json'
import Button from './Button.json'
import Calendar from './Calendar.json'
import CalendarView from './CalendarView.json'
import Capsule from './Capsule.json'
import Card from './Card.json'
import Cell from './Cell.json'
import CellGroup from './CellGroup.json'
import Checkbox from './Checkbox.json'
import CheckboxGroup from './CheckboxGroup.json'
import Circle from './Circle.json'
import Col from './Col.json'
import Collapse from './Collapse.json'
import CollapseItem from './CollapseItem.json'
import ColPicker from './ColPicker.json'
import ConfigProvider from './ConfigProvider.json'
import CountDown from './CountDown.json'
import CountTo from './CountTo.json'
import Curtain from './Curtain.json'
import DatetimePicker from './DatetimePicker.json'
import DatetimePickerView from './DatetimePickerView.json'
import Divider from './Divider.json'
import DropdownItem from './DropdownItem.json'
import DropMenu from './DropMenu.json'
import DropMenuItem from './DropMenuItem.json'
import Fab from './Fab.json'
import FloatingPanel from './FloatingPanel.json'
import Form from './Form.json'
import Gap from './Gap.json'
import Grid from './Grid.json'
import GridItem from './GridItem.json'
import Icon from './Icon.json'
import Img from './Img.json'
import ImgCropper from './ImgCropper.json'
import IndexAnchor from './IndexAnchor.json'
import IndexBar from './IndexBar.json'
import Input from './Input.json'
import InputNumber from './InputNumber.json'
import Keyboard from './Keyboard.json'
import Loading from './Loading.json'
import loadmore from './loadmore.json'
import Navbar from './Navbar.json'
import NoticeBar from './NoticeBar.json'
import Notify from './Notify.json'
import NumberKeyboard from './NumberKeyboard.json'
import Options from './Options.json'
import Overlay from './Overlay.json'
import Pagination from './Pagination.json'
import PasswordInput from './PasswordInput.json'
import Picker from './Picker.json'
import PickerView from './PickerView.json'
import Popover from './Popover.json'
import Popup from './Popup.json'
import Progress from './Progress.json'
import Radio from './Radio.json'
import RadioGroup from './RadioGroup.json'
import Rate from './Rate.json'
import Resize from './Resize.json'
import Row from './Row.json'
import Search from './Search.json'
import Segmented from './Segmented.json'
import SelectPicker from './SelectPicker.json'
import Sidebar from './Sidebar.json'
import SidebarItem from './SidebarItem.json'
import Skeleton from './Skeleton.json'
import Slider from './Slider.json'
import SortButton from './SortButton.json'
import StatusTip from './StatusTip.json'
import Step from './Step.json'
import Steps from './Steps.json'
import Sticky from './Sticky.json'
import SwipeAction from './SwipeAction.json'
import Swiper from './Swiper.json'
import Switch from './Switch.json'
import Tab from './Tab.json'
import Tabbar from './Tabbar.json'
import TabbarItem from './TabbarItem.json'
import Table from './Table.json'
import TableColumn from './TableColumn.json'
import Tabs from './Tabs.json'
import Tag from './Tag.json'
import Text from './Text.json'
import Textarea from './Textarea.json'
import Toast from './Toast.json'
import Tooltip from './Tooltip.json'
import Upload from './Upload.json'
import Watermark from './Watermark.json'

export function getPropsMap() {
  return [
    ActionSheet,
    Backtop,
    Badge,
    Button,
    Calendar,
    CalendarView,
    Capsule,
    Card,
    Cell,
    CellGroup,
    Checkbox,
    CheckboxGroup,
    Circle,
    Col,
    ColPicker,
    Collapse,
    CollapseItem,
    ConfigProvider,
    CountDown,
    CountTo,
    Curtain,
    DatetimePicker,
    DatetimePickerView,
    Divider,
    DropMenu,
    DropMenuItem,
    DropdownItem,
    Fab,
    FloatingPanel,
    Form,
    Gap,
    Grid,
    GridItem,
    Icon,
    Img,
    ImgCropper,
    IndexAnchor,
    IndexBar,
    Input,
    InputNumber,
    Keyboard,
    Loading,
    Navbar,
    NoticeBar,
    Notify,
    NumberKeyboard,
    Options,
    Overlay,
    Pagination,
    PasswordInput,
    Picker,
    PickerView,
    Popover,
    Popup,
    Progress,
    Radio,
    RadioGroup,
    Rate,
    Resize,
    Row,
    Search,
    Segmented,
    SelectPicker,
    Sidebar,
    SidebarItem,
    Skeleton,
    Slider,
    SortButton,
    StatusTip,
    Step,
    Steps,
    Sticky,
    SwipeAction,
    Swiper,
    Switch,
    Tab,
    Tabbar,
    TabbarItem,
    Table,
    TableColumn,
    Tabs,
    Tag,
    Text,
    Textarea,
    Toast,
    Tooltip,
    Upload,
    Watermark,
    loadmore,
  ]
}

export function getComponentsMap() {
  return [
    [ActionSheet, ActionSheet.name, `<${hyphenate(ActionSheet.name)}></${hyphenate(ActionSheet.name)}>`],
    [Backtop, Backtop.name, `<${hyphenate(Backtop.name)}></${hyphenate(Backtop.name)}>`],
    [Badge, Badge.name, `<${hyphenate(Badge.name)}></${hyphenate(Badge.name)}>`],
    [Button, Button.name, `<${hyphenate(Button.name)}></${hyphenate(Button.name)}>`],
    [Calendar, Calendar.name, `<${hyphenate(Calendar.name)}></${hyphenate(Calendar.name)}>`],
    [CalendarView, CalendarView.name, `<${hyphenate(CalendarView.name)}></${hyphenate(CalendarView.name)}>`],
    [Capsule, Capsule.name, `<${hyphenate(Capsule.name)}></${hyphenate(Capsule.name)}>`],
    [Card, Card.name, `<${hyphenate(Card.name)}></${hyphenate(Card.name)}>`],
    [Cell, Cell.name, `<${hyphenate(Cell.name)}></${hyphenate(Cell.name)}>`],
    [CellGroup, CellGroup.name, `<${hyphenate(CellGroup.name)}></${hyphenate(CellGroup.name)}>`],
    [Checkbox, Checkbox.name, `<${hyphenate(Checkbox.name)}></${hyphenate(Checkbox.name)}>`],
    [CheckboxGroup, CheckboxGroup.name, `<${hyphenate(CheckboxGroup.name)}></${hyphenate(CheckboxGroup.name)}>`],
    [Circle, Circle.name, `<${hyphenate(Circle.name)}></${hyphenate(Circle.name)}>`],
    [Col, Col.name, `<${hyphenate(Col.name)}></${hyphenate(Col.name)}>`],
    [ColPicker, ColPicker.name, `<${hyphenate(ColPicker.name)}></${hyphenate(ColPicker.name)}>`],
    [Collapse, Collapse.name, `<${hyphenate(Collapse.name)}></${hyphenate(Collapse.name)}>`],
    [CollapseItem, CollapseItem.name, `<${hyphenate(CollapseItem.name)}></${hyphenate(CollapseItem.name)}>`],
    [ConfigProvider, ConfigProvider.name, `<${hyphenate(ConfigProvider.name)}></${hyphenate(ConfigProvider.name)}>`],
    [CountDown, CountDown.name, `<${hyphenate(CountDown.name)}></${hyphenate(CountDown.name)}>`],
    [CountTo, CountTo.name, `<${hyphenate(CountTo.name)}></${hyphenate(CountTo.name)}>`],
    [Curtain, Curtain.name, `<${hyphenate(Curtain.name)}></${hyphenate(Curtain.name)}>`],
    [DatetimePicker, DatetimePicker.name, `<${hyphenate(DatetimePicker.name)}></${hyphenate(DatetimePicker.name)}>`],
    [DatetimePickerView, DatetimePickerView.name, `<${hyphenate(DatetimePickerView.name)}></${hyphenate(DatetimePickerView.name)}>`],
    [Divider, Divider.name, `<${hyphenate(Divider.name)}></${hyphenate(Divider.name)}>`],
    [DropMenu, DropMenu.name, `<${hyphenate(DropMenu.name)}></${hyphenate(DropMenu.name)}>`],
    [DropMenuItem, DropMenuItem.name, `<${hyphenate(DropMenuItem.name)}></${hyphenate(DropMenuItem.name)}>`],
    [DropdownItem, DropdownItem.name, `<${hyphenate(DropdownItem.name)}></${hyphenate(DropdownItem.name)}>`],
    [Fab, Fab.name, `<${hyphenate(Fab.name)}></${hyphenate(Fab.name)}>`],
    [FloatingPanel, FloatingPanel.name, `<${hyphenate(FloatingPanel.name)}></${hyphenate(FloatingPanel.name)}>`],
    [Form, Form.name, `<${hyphenate(Form.name)}></${hyphenate(Form.name)}>`],
    [Gap, Gap.name, `<${hyphenate(Gap.name)}></${hyphenate(Gap.name)}>`],
    [Grid, Grid.name, `<${hyphenate(Grid.name)}></${hyphenate(Grid.name)}>`],
    [GridItem, GridItem.name, `<${hyphenate(GridItem.name)}></${hyphenate(GridItem.name)}>`],
    [Icon, Icon.name, `<${hyphenate(Icon.name)}></${hyphenate(Icon.name)}>`],
    [Img, Img.name, `<${hyphenate(Img.name)}></${hyphenate(Img.name)}>`],
    [ImgCropper, ImgCropper.name, `<${hyphenate(ImgCropper.name)}></${hyphenate(ImgCropper.name)}>`],
    [IndexAnchor, IndexAnchor.name, `<${hyphenate(IndexAnchor.name)}></${hyphenate(IndexAnchor.name)}>`],
    [IndexBar, IndexBar.name, `<${hyphenate(IndexBar.name)}></${hyphenate(IndexBar.name)}>`],
    [Input, Input.name, `<${hyphenate(Input.name)}></${hyphenate(Input.name)}>`],
    [InputNumber, InputNumber.name, `<${hyphenate(InputNumber.name)}></${hyphenate(InputNumber.name)}>`],
    [Keyboard, Keyboard.name, `<${hyphenate(Keyboard.name)}></${hyphenate(Keyboard.name)}>`],
    [Loading, Loading.name, `<${hyphenate(Loading.name)}></${hyphenate(Loading.name)}>`],
    [Navbar, Navbar.name, `<${hyphenate(Navbar.name)}></${hyphenate(Navbar.name)}>`],
    [NoticeBar, NoticeBar.name, `<${hyphenate(NoticeBar.name)}></${hyphenate(NoticeBar.name)}>`],
    [Notify, Notify.name, `<${hyphenate(Notify.name)}></${hyphenate(Notify.name)}>`],
    [NumberKeyboard, NumberKeyboard.name, `<${hyphenate(NumberKeyboard.name)}></${hyphenate(NumberKeyboard.name)}>`],
    [Options, Options.name, `<${hyphenate(Options.name)}></${hyphenate(Options.name)}>`],
    [Overlay, Overlay.name, `<${hyphenate(Overlay.name)}></${hyphenate(Overlay.name)}>`],
    [Pagination, Pagination.name, `<${hyphenate(Pagination.name)}></${hyphenate(Pagination.name)}>`],
    [PasswordInput, PasswordInput.name, `<${hyphenate(PasswordInput.name)}></${hyphenate(PasswordInput.name)}>`],
    [Picker, Picker.name, `<${hyphenate(Picker.name)}></${hyphenate(Picker.name)}>`],
    [PickerView, PickerView.name, `<${hyphenate(PickerView.name)}></${hyphenate(PickerView.name)}>`],
    [Popover, Popover.name, `<${hyphenate(Popover.name)}></${hyphenate(Popover.name)}>`],
    [Popup, Popup.name, `<${hyphenate(Popup.name)}></${hyphenate(Popup.name)}>`],
    [Progress, Progress.name, `<${hyphenate(Progress.name)}></${hyphenate(Progress.name)}>`],
    [Radio, Radio.name, `<${hyphenate(Radio.name)}></${hyphenate(Radio.name)}>`],
    [RadioGroup, RadioGroup.name, `<${hyphenate(RadioGroup.name)}></${hyphenate(RadioGroup.name)}>`],
    [Rate, Rate.name, `<${hyphenate(Rate.name)}></${hyphenate(Rate.name)}>`],
    [Resize, Resize.name, `<${hyphenate(Resize.name)}></${hyphenate(Resize.name)}>`],
    [Row, Row.name, `<${hyphenate(Row.name)}></${hyphenate(Row.name)}>`],
    [Search, Search.name, `<${hyphenate(Search.name)}></${hyphenate(Search.name)}>`],
    [Segmented, Segmented.name, `<${hyphenate(Segmented.name)}></${hyphenate(Segmented.name)}>`],
    [SelectPicker, SelectPicker.name, `<${hyphenate(SelectPicker.name)}></${hyphenate(SelectPicker.name)}>`],
    [Sidebar, Sidebar.name, `<${hyphenate(Sidebar.name)}></${hyphenate(Sidebar.name)}>`],
    [SidebarItem, SidebarItem.name, `<${hyphenate(SidebarItem.name)}></${hyphenate(SidebarItem.name)}>`],
    [Skeleton, Skeleton.name, `<${hyphenate(Skeleton.name)}></${hyphenate(Skeleton.name)}>`],
    [Slider, Slider.name, `<${hyphenate(Slider.name)}></${hyphenate(Slider.name)}>`],
    [SortButton, SortButton.name, `<${hyphenate(SortButton.name)}></${hyphenate(SortButton.name)}>`],
    [StatusTip, StatusTip.name, `<${hyphenate(StatusTip.name)}></${hyphenate(StatusTip.name)}>`],
    [Step, Step.name, `<${hyphenate(Step.name)}></${hyphenate(Step.name)}>`],
    [Steps, Steps.name, `<${hyphenate(Steps.name)}></${hyphenate(Steps.name)}>`],
    [Sticky, Sticky.name, `<${hyphenate(Sticky.name)}></${hyphenate(Sticky.name)}>`],
    [SwipeAction, SwipeAction.name, `<${hyphenate(SwipeAction.name)}></${hyphenate(SwipeAction.name)}>`],
    [Swiper, Swiper.name, `<${hyphenate(Swiper.name)}></${hyphenate(Swiper.name)}>`],
    [Switch, Switch.name, `<${hyphenate(Switch.name)}></${hyphenate(Switch.name)}>`],
    [Tab, Tab.name, `<${hyphenate(Tab.name)}></${hyphenate(Tab.name)}>`],
    [Tabbar, Tabbar.name, `<${hyphenate(Tabbar.name)}></${hyphenate(Tabbar.name)}>`],
    [TabbarItem, TabbarItem.name, `<${hyphenate(TabbarItem.name)}></${hyphenate(TabbarItem.name)}>`],
    [Table, Table.name, `<${hyphenate(Table.name)}></${hyphenate(Table.name)}>`],
    [TableColumn, TableColumn.name, `<${hyphenate(TableColumn.name)}></${hyphenate(TableColumn.name)}>`],
    [Tabs, Tabs.name, `<${hyphenate(Tabs.name)}></${hyphenate(Tabs.name)}>`],
    [Tag, Tag.name, `<${hyphenate(Tag.name)}></${hyphenate(Tag.name)}>`],
    [Text, Text.name, `<${hyphenate(Text.name)}></${hyphenate(Text.name)}>`],
    [Textarea, Textarea.name, `<${hyphenate(Textarea.name)}></${hyphenate(Textarea.name)}>`],
    [Toast, Toast.name, `<${hyphenate(Toast.name)}></${hyphenate(Toast.name)}>`],
    [Tooltip, Tooltip.name, `<${hyphenate(Tooltip.name)}></${hyphenate(Tooltip.name)}>`],
    [Upload, Upload.name, `<${hyphenate(Upload.name)}></${hyphenate(Upload.name)}>`],
    [Watermark, Watermark.name, `<${hyphenate(Watermark.name)}></${hyphenate(Watermark.name)}>`],
    [loadmore, loadmore.name, `<${hyphenate(loadmore.name)}></${hyphenate(loadmore.name)}>`],
  ]
}
export function hyphenate(s: string): string {
  return s.replace(/([A-Z])/g, '-$1').toLowerCase().replace(/^-/, '')
}

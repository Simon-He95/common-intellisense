import { componentsReducer, propsReducer } from '../../utils'

export function daisyui3() {
  const map: any = [
  ]

  return propsReducer(map)
}

export function daisyui3Components() {
  const map = [
    [],
  ]
  return componentsReducer(map)
}

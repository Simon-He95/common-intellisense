{
  "name": "NFormItemGi",
  "props": {
    "feedback": {
      "value": "",
      "description": "表项的反馈信息。不设为 `undefined` 时，会覆盖规则验证的结果",
      "default": "undefined",
      "type": "string"
    },
    "first": {
      "value": "",
      "description": "是否只展示首个出错信息",
      "default": "false",
      "type": "boolean"
    },
    "ignore-path-change": {
      "value": "",
      "description": "通常 `path` 的改变会导致数据来源的变化，所以 naive-ui 会清空验证信息。如果不期望这个行为，可以将其置为 `true`",
      "default": "false",
      "type": "boolean"
    },
    "label": {
      "value": "",
      "description": "标签信息",
      "default": "undefined",
      "type": "string"
    },
    "label-align": {
      "value": ["left", "right"],
      "description": "标签的文本对齐方式。如果没有被设定，使用外层表单的 `label-align`",
      "default": "undefined",
      "type": "'left' | 'right'"
    },
    "label-placement": {
      "value": ["left", "top"],
      "description": "如果没有被设定，使用外层表单的 `label-placement`",
      "default": "undefined",
      "type": "'left' | 'top'"
    },
    "label-style": {
      "value": "",
      "description": "标签的样式",
      "default": "undefined",
      "type": "CSSProperties | string"
    },
    ":label-props": {
      "value": "",
      "description": "标签元素的属性",
      "default": "undefined",
      "type": "LabelHTMLAttributes"
    },
    "label-width": {
      "value": "",
      "description": "如果没有被设定，使用外层表单的 `label-width`，`'auto'` 意味着 label width 会被自动调整",
      "default": "undefined",
      "type": "number | string | 'auto'"
    },
    "path": {
      "value": "",
      "description": "将值收集到外层表单 `model` 对象的路径",
      "default": "undefined",
      "type": "string"
    },
    ":rule": {
      "value": "",
      "description": "验证表项的规则，它会被通过 `rule-path` 从外层表单获取的规则合并来作为表项的验证规则。推荐还是在外层表单设置所有规则",
      "default": "undefined",
      "type": "FormItemRule | Array<FormItemRule>"
    },
    "rule-path": {
      "value": "",
      "description": "从外层表单的 `rules` 对象获取规则的路径。如果没有设定，使用表项的 `path` 代替",
      "default": "undefined",
      "type": "string"
    },
    "show-feedback": {
      "value": "",
      "description": "是否展示校验反馈",
      "default": "true",
      "type": "boolean"
    },
    "show-label": {
      "value": "",
      "description": "是否展示标签。如果没有被设定，使用外层 `n-form` 的 `show-label`",
      "default": "true",
      "type": "boolean"
    },
    "show-require-mark": {
      "value": "",
      "description": "是否展示必填的星号。如果没有被设定，使用外层 `n-form` 的 `show-require-mark`",
      "default": "false",
      "type": "boolean"
    },
    "require-mark-placement": {
      "value": ["left", "right", "right-hanging"],
      "description": "必填的星号的位置。如果没有被设定，使用外层 `n-form` 的 `require-mark-placement`",
      "default": "'right'",
      "type": "'left' | 'right' | 'right-hanging'"
    },
    "size": {
      "value": ["small", "medium", "large"],
      "description": "尺寸",
      "default": "'medium'",
      "type": "'small' | 'medium' | 'large'"
    },
    "validation-status": {
      "value": ["error", "success", "warning"],
      "description": "表单的验证状态。不设为 `undefined`时，会覆盖规则验证的结果",
      "default": "undefined",
      "type": "'error' | 'success' | 'warning'"
    },
    "offset": {
      "value": "",
      "description": "栅格左侧的间隔格数，",
      "default": "0",
      "type": "number | ResponsiveDescription"
    },
    "span": {
      "value": "",
      "description": "栅格占据的列数，为 0 的时候会隐藏",
      "default": "1",
      "type": "number | ResponsiveDescription"
    },
    "suffix": {
      "value": "",
      "description": "栅格后缀",
      "default": "false",
      "type": "boolean"
    }
  },
  "methods": [
    {
      "name": "validate",
      "description": "验证表项，Promise rejection 的返回值类型是 `Array<FormValidationError>`。如果不设定 `trigger`，这一个表项全部的规则都会被使用。`shouldRuleBeApplied` 可以用来进一步过滤已经经过 `trigger` 筛选的规则",
      "params": "`(options: { trigger?: string, callback?: (errors?: Array<FormValidationError>) => void, shouldRuleBeApplied?: FormItemRule => boolean, options?: AsyncValidatorOptions }) => Promise<void>`"
    },
    {
      "name": "restoreValidation",
      "description": "还原到未校验的状态",
      "params": "`() => void`"
    }
  ],
  "typeDetail": {},
  "events": [],
  "link": "https://www.naiveui.com/zh-CN/dark/components/form"
}

{
  "name": "AlertDialog.Root",
  "props": {
    "preventScroll": {
      "value": "",
      "default": "true",
      "type": "boolean",
      "description": "Whether or not to prevent scroll on the body when the alert dialog is open.",
      "description_zh": "Whether or not to prevent scroll on the body when the alert dialog is open."
    },
    "closeOnEscape": {
      "value": "",
      "default": "true",
      "type": "boolean",
      "description": "Whether to close the alert dialog when the escape key is pressed.",
      "description_zh": "Whether to close the alert dialog when the escape key is pressed."
    },
    "closeOnOutsideClick": {
      "value": "",
      "default": "false",
      "type": "boolean",
      "description": "Whether to close the alert dialog when a click occurs outside of it.",
      "description_zh": "Whether to close the alert dialog when a click occurs outside of it."
    },
    "open": {
      "value": "",
      "default": "false",
      "type": "boolean",
      "description": "Whether or not the alert dialog is open.",
      "description_zh": "Whether or not the alert dialog is open."
    },
    "onOpenChange": {
      "value": "",
      "default": "_",
      "type": "function",
      "description": "A callback function called when the open state changes.",
      "description_zh": "A callback function called when the open state changes."
    },
    "openFocus": {
      "value": "",
      "default": "_",
      "type": "FocusProp",
      "description": "Override the initial focus when the alert dialog is opened.",
      "description_zh": "Override the initial focus when the alert dialog is opened."
    },
    "closeFocus": {
      "value": "",
      "default": "_",
      "type": "FocusProp",
      "description": "Override the focus when the alert dialog is closed.",
      "description_zh": "Override the focus when the alert dialog is closed."
    },
    "portal": {
      "value": "",
      "default": "_",
      "type": "union",
      "description": "Where to render the alert dialog when it is open. Defaults to the body. Can be disabled by passing null",
      "description_zh": "Where to render the alert dialog when it is open. Defaults to the body. Can be disabled by passing null"
    },
    "onOutsideClick": {
      "value": "",
      "default": "_",
      "type": "function",
      "description": "A callback function called when a click occurs outside of the element. If event.preventDefault() is called, the default behavior of closing the element will be prevented.",
      "description_zh": "A callback function called when a click occurs outside of the element. If event.preventDefault() is called, the default behavior of closing the element will be prevented."
    }
  },
  "description": "The root component used to set and manage the state of the alert dialog.",
  "link": "https://www.bits-ui.com/docs/components/alert-dialog",
  "link_zh": "https://www.bits-ui.com/docs/components/alert-dialog",
  "typeDetail": {},
  "events": [],
  "methods": [],
  "slots": [
    {
      "name": "ids",
      "description": "The ids of the elements within the component, useful when you don't necessarily want to provide a custom ID, but still want access to the ID being assigned (if any). ",
      "description_zh": "The ids of the elements within the component, useful when you don't necessarily want to provide a custom ID, but still want access to the ID being assigned (if any). "
    }
  ],
  "filename": "AlertDialogRoot"
}

import { componentsReducer, propsReducer } from '../../utils'

export function chakraUi0() {
  const map: any = [
  ]

  return propsReducer(map)
}

export function chakraUi0Components() {
  const map = [
    ['AspectRatio', 'AspectRatio component is used to embed responsive videos and maps, etc.'],
  ]
  return componentsReducer(map)
}

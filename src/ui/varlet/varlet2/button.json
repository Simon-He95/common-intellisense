{
  "name": "VarButton",
  "props": {
    "type": {
      "value": ["default", "primary", "info", "success", "warning", "danger"],
      "description": "类型，可选值为 `default` `primary` `info` `success` `warning` danger",
      "default": "default",
      "type": "string"
    },
    "native-type": {
      "value": ["button", "submit", "reset"],
      "description": "原生类型，可选值为 `button` `submit` reset",
      "default": "button",
      "type": "string"
    },
    "size": {
      "value": ["large", "normal", "small", "mini"],
      "description": "尺寸，可选值为 `normal` `mini` `small` large",
      "default": "normal",
      "type": "string"
    },
    "loading": {
      "value": "",
      "description": "加载状态",
      "default": "false",
      "type": "boolean"
    },
    "loading-radius": {
      "value": "",
      "description": "loading 的半径，只作用于 `loading-type=\"circle\"` 时",
      "default": "-",
      "type": "number"
    },
    "loading-type": {
      "value": ["circle", "wave", "cube", "rect", "disappear"],
      "description": "loading 的类型，可选值为 `circle` `wave` `cube` `rect` disappear",
      "default": "circle",
      "type": "string"
    },
    "loading-size": {
      "value": ["large", "normal", "small", "mini"],
      "description": "loading 的尺寸，可选值为 `large` `normal` `small` mini",
      "default": "normal",
      "type": "string"
    },
    "loading-color": {
      "value": "",
      "description": "loading 的颜色",
      "default": "currentColor",
      "type": "string"
    },
    "auto-loading": {
      "value": "",
      "description": "自动 loading 模式，方便处理异步任务",
      "default": "false",
      "type": "boolean"
    },
    "round": {
      "value": "",
      "description": "是否是圆形按钮",
      "default": "false",
      "type": "boolean"
    },
    "block": {
      "value": "",
      "description": "是否是块级元素",
      "default": "false",
      "type": "boolean"
    },
    "text": {
      "value": "",
      "description": "是否是文字按钮",
      "default": "false",
      "type": "boolean"
    },
    "outline": {
      "value": "",
      "description": "是否使用外边框",
      "default": "false",
      "type": "boolean"
    },
    "disabled": {
      "value": "",
      "description": "禁用状态",
      "default": "false",
      "type": "boolean"
    },
    "ripple": {
      "value": "",
      "description": "是否使用水波纹",
      "default": "true",
      "type": "boolean"
    },
    "text-color": {
      "value": "",
      "description": "文字颜色",
      "default": "-",
      "type": "string"
    },
    "color": {
      "value": "",
      "description": "背景颜色",
      "default": "-",
      "type": "string"
    },
    "elevation": {
      "value": ["true", "false", "0-24"],
      "description": "海拔高度，可选值为 `true`、`false` 和 `0-24` 的等级",
      "default": "true",
      "type": "boolean"
    }
  },
  "methods": [],
  "typeDetail": {},
  "events": [
    {
      "name": "click",
      "description": "点击按钮时触发，在 `loading` 或 `disabled` 状态为 `true` 时不触发",
      "params": "点击按钮时触发，在 `loading` 或 `disabled` 状态为 `true` 时不触发"
    },
    {
      "name": "touchstart",
      "description": "触摸手指压下按钮时触发，在 `loading` 或 `disabled` 状态为 `true` 时不触发",
      "params": "触摸手指压下按钮时触发，在 `loading` 或 `disabled` 状态为 `true` 时不触发"
    }
  ],
  "link": "https://varlet.gitee.io/varlet-ui/#/zh-CN/button"
}

{
  "name": "VarButton",
  "props": {
    "type": {
      "value": "",
      "description": "类型，可选值为 `default` `primary` `info` `success` `warning` `danger`",
      "default": "default",
      "type": "类型，可选值为 default primary info success warning danger"
    },
    "native-type": {
      "value": "",
      "description": "原生类型，可选值为 `button` `submit` `reset`",
      "default": "button",
      "type": "原生类型，可选值为 button submit reset"
    },
    "size": {
      "value": "",
      "description": "尺寸，可选值为 `normal` `mini` `small` `large`",
      "default": "normal",
      "type": "尺寸，可选值为 normal mini small large"
    },
    "loading": {
      "value": "",
      "description": "加载状态",
      "default": "false",
      "type": "加载状态"
    },
    "loading-radius": {
      "value": "",
      "description": "loading 的半径，只作用于 `loading-type=\"circle\"` 时",
      "default": "-",
      "type": "loading 的半径，只作用于 loading-type=\"circle\" 时"
    },
    "loading-type": {
      "value": "",
      "description": "loading 的类型，可选值为 `circle` `wave` `cube` `rect` `disappear`",
      "default": "circle",
      "type": "loading 的类型，可选值为 circle wave cube rect disappear"
    },
    "loading-size": {
      "value": "",
      "description": "loading 的尺寸，可选值为 `large` `normal` `small` `mini`",
      "default": "normal",
      "type": "loading 的尺寸，可选值为 large normal small mini"
    },
    "loading-color": {
      "value": "",
      "description": "loading 的颜色",
      "default": "currentColor",
      "type": "loading 的颜色"
    },
    "auto-loading": {
      "value": "",
      "description": "自动 loading 模式，方便处理异步任务",
      "default": "false",
      "type": "自动 loading 模式，方便处理异步任务"
    },
    "round": {
      "value": "",
      "description": "是否是圆形按钮",
      "default": "false",
      "type": "是否是圆形按钮"
    },
    "block": {
      "value": "",
      "description": "是否是块级元素",
      "default": "false",
      "type": "是否是块级元素"
    },
    "text": {
      "value": "",
      "description": "是否是文字按钮",
      "default": "false",
      "type": "是否是文字按钮"
    },
    "outline": {
      "value": "",
      "description": "是否使用外边框",
      "default": "false",
      "type": "是否使用外边框"
    },
    "disabled": {
      "value": "",
      "description": "禁用状态",
      "default": "false",
      "type": "禁用状态"
    },
    "ripple": {
      "value": "",
      "description": "是否使用水波纹",
      "default": "true",
      "type": "是否使用水波纹"
    },
    "text-color": {
      "value": "",
      "description": "文字颜色",
      "default": "-",
      "type": "文字颜色"
    },
    "color": {
      "value": "",
      "description": "背景颜色",
      "default": "-",
      "type": "背景颜色"
    },
    "elevation": {
      "value": "",
      "description": "海拔高度，可选值为 `true`、`false` 和 `0-24` 的等级",
      "default": "true",
      "type": "海拔高度，可选值为 true、false 和 0-24 的等级"
    }
  },
  "methods": [],
  "typeDetail": {},
  "events": [
    {
      "name": "click",
      "description": "点击按钮时触发，在 `loading` 或 `disabled` 状态为 `true` 时不触发",
      "params": "点击按钮时触发，在 `loading` 或 `disabled` 状态为 `true` 时不触发"
    },
    {
      "name": "touchstart",
      "description": "触摸手指压下按钮时触发，在 `loading` 或 `disabled` 状态为 `true` 时不触发",
      "params": "触摸手指压下按钮时触发，在 `loading` 或 `disabled` 状态为 `true` 时不触发"
    }
  ],
  "link": "https://varlet.gitee.io/varlet-ui/#/zh-CN/button"
}

{
  "name": "VanList",
  "props": {
    "v-model:loading": {
      "value": "",
      "description": "是否处于加载状态，加载过程中不触发 `load` 事件",
      "default": "false",
      "type": "是否处于加载状态，加载过程中不触发 load 事件"
    },
    "v-model:error": {
      "value": "",
      "description": "是否加载失败，加载失败后点击错误提示可以重新触发 `load` 事件",
      "default": "false",
      "type": "是否加载失败，加载失败后点击错误提示可以重新触发 load 事件"
    },
    "finished": {
      "value": "",
      "description": "是否已加载完成，加载完成后不再触发 `load` 事件",
      "default": "false",
      "type": "是否已加载完成，加载完成后不再触发 load 事件"
    },
    "offset": {
      "value": "",
      "description": "滚动条与底部距离小于 offset 时触发 `load` 事件",
      "default": "300",
      "type": "滚动条与底部距离小于 offset 时触发 load 事件"
    },
    "loading-text": {
      "value": "",
      "description": "加载过程中的提示文案",
      "default": "加载中...",
      "type": "加载过程中的提示文案"
    },
    "finished-text": {
      "value": "",
      "description": "加载完成后的提示文案",
      "default": "-",
      "type": "加载完成后的提示文案"
    },
    "error-text": {
      "value": "",
      "description": "加载失败后的提示文案",
      "default": "-",
      "type": "加载失败后的提示文案"
    },
    "immediate-check": {
      "value": "",
      "description": "是否在初始化时立即执行滚动位置检查",
      "default": "true",
      "type": "是否在初始化时立即执行滚动位置检查"
    },
    "disabled": {
      "value": "",
      "description": "是否禁用滚动加载",
      "default": "false",
      "type": "是否禁用滚动加载"
    },
    "direction": {
      "value": "",
      "description": "滚动触发加载的方向，可选值为 `up`",
      "default": "down",
      "type": "滚动触发加载的方向，可选值为 up"
    }
  },
  "methods": [],
  "typeDetail": {},
  "events": [
    {
      "name": "load",
      "description": "滚动条与底部距离小于 offset 时触发",
      "params": "滚动条与底部距离小于 offset 时触发"
    }
  ],
  "link": "https://vant-contrib.gitee.io/vant/#/zh-CN/list"
}

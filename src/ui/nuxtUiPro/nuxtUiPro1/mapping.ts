import Aside from './Aside.json'
import AsideLinks from './AsideLinks.json'
import AuthForm from './AuthForm.json'
import BlogList from './BlogList.json'
import BlogPost from './BlogPost.json'
import Callout from './Callout.json'
import Card from './Card.json'
import CardGroup from './CardGroup.json'
import CodeBlock from './CodeBlock.json'
import CodeGroup from './CodeGroup.json'
import Collapsible from './Collapsible.json'
import ColorModeAvatar from './ColorModeAvatar.json'
import ColorModeButton from './ColorModeButton.json'
import ColorModeImage from './ColorModeImage.json'
import ColorModeSelect from './ColorModeSelect.json'
import ColorModeToggle from './ColorModeToggle.json'
import ContentSearch from './ContentSearch.json'
import ContentSearchButton from './ContentSearchButton.json'
import ContentSurround from './ContentSurround.json'
import ContentToc from './ContentToc.json'
import DashboardCard from './DashboardCard.json'
import DashboardLayout from './DashboardLayout.json'
import DashboardModal from './DashboardModal.json'
import DashboardNavbar from './DashboardNavbar.json'
import DashboardNavbarToggle from './DashboardNavbarToggle.json'
import DashboardPage from './DashboardPage.json'
import DashboardPanel from './DashboardPanel.json'
import DashboardPanelContent from './DashboardPanelContent.json'
import DashboardPanelHandle from './DashboardPanelHandle.json'
import DashboardSearch from './DashboardSearch.json'
import DashboardSearchButton from './DashboardSearchButton.json'
import DashboardSection from './DashboardSection.json'
import DashboardSidebar from './DashboardSidebar.json'
import DashboardSidebarLinks from './DashboardSidebarLinks.json'
import DashboardSlideover from './DashboardSlideover.json'
import DashboardToolbar from './DashboardToolbar.json'
import Field from './Field.json'
import FieldGroup from './FieldGroup.json'
import Footer from './Footer.json'
import FooterColumns from './FooterColumns.json'
import FooterLinks from './FooterLinks.json'
import Header from './Header.json'
import HeaderLinks from './HeaderLinks.json'
import HeaderPopoverLinks from './HeaderPopoverLinks.json'
import LandingCard from './LandingCard.json'
import LandingCTA from './LandingCTA.json'
import LandingFAQ from './LandingFAQ.json'
import LandingGrid from './LandingGrid.json'
import LandingHero from './LandingHero.json'
import LandingLogos from './LandingLogos.json'
import LandingSection from './LandingSection.json'
import LandingTestimonial from './LandingTestimonial.json'
import Main from './Main.json'
import NavigationAccordion from './NavigationAccordion.json'
import NavigationLinks from './NavigationLinks.json'
import NavigationTree from './NavigationTree.json'
import Page from './Page.json'
import PageBody from './PageBody.json'
import PageCard from './PageCard.json'
import PageColumns from './PageColumns.json'
import PageError from './PageError.json'
import PageGrid from './PageGrid.json'
import PageHeader from './PageHeader.json'
import PageHero from './PageHero.json'
import PageLinks from './PageLinks.json'
import PricingCard from './PricingCard.json'
import PricingGrid from './PricingGrid.json'
import PricingToggle from './PricingToggle.json'
import Shortcut from './Shortcut.json'
import Tabs from './Tabs.json'

export function getPropsMap() {
  return [
    Aside,
    AsideLinks,
    AuthForm,
    BlogList,
    BlogPost,
    Callout,
    Card,
    CardGroup,
    CodeBlock,
    CodeGroup,
    Collapsible,
    ColorModeAvatar,
    ColorModeButton,
    ColorModeImage,
    ColorModeSelect,
    ColorModeToggle,
    ContentSearch,
    ContentSearchButton,
    ContentSurround,
    ContentToc,
    DashboardCard,
    DashboardLayout,
    DashboardModal,
    DashboardNavbar,
    DashboardNavbarToggle,
    DashboardPage,
    DashboardPanel,
    DashboardPanelContent,
    DashboardPanelHandle,
    DashboardSearch,
    DashboardSearchButton,
    DashboardSection,
    DashboardSidebar,
    DashboardSidebarLinks,
    DashboardSlideover,
    DashboardToolbar,
    Field,
    FieldGroup,
    Footer,
    FooterColumns,
    FooterLinks,
    Header,
    HeaderLinks,
    HeaderPopoverLinks,
    LandingCTA,
    LandingCard,
    LandingFAQ,
    LandingGrid,
    LandingHero,
    LandingLogos,
    LandingSection,
    LandingTestimonial,
    Main,
    NavigationAccordion,
    NavigationLinks,
    NavigationTree,
    Page,
    PageBody,
    PageCard,
    PageColumns,
    PageError,
    PageGrid,
    PageHeader,
    PageHero,
    PageLinks,
    PricingCard,
    PricingGrid,
    PricingToggle,
    Shortcut,
    Tabs,
  ]
}

export function getComponentMap() {
  return [
    [Aside, Aside.name, `<${Aside.name}></${Aside.name}`],
    [AsideLinks, AsideLinks.name, `<${AsideLinks.name}></${AsideLinks.name}`],
    [AuthForm, AuthForm.name, `<${AuthForm.name}></${AuthForm.name}`],
    [BlogList, BlogList.name, `<${BlogList.name}></${BlogList.name}`],
    [BlogPost, BlogPost.name, `<${BlogPost.name}></${BlogPost.name}`],
    [Callout, Callout.name, `<${Callout.name}></${Callout.name}`],
    [Card, Card.name, `<${Card.name}></${Card.name}`],
    [CardGroup, CardGroup.name, `<${CardGroup.name}></${CardGroup.name}`],
    [CodeBlock, CodeBlock.name, `<${CodeBlock.name}></${CodeBlock.name}`],
    [CodeGroup, CodeGroup.name, `<${CodeGroup.name}></${CodeGroup.name}`],
    [Collapsible, Collapsible.name, `<${Collapsible.name}></${Collapsible.name}`],
    [ColorModeAvatar, ColorModeAvatar.name, `<${ColorModeAvatar.name}></${ColorModeAvatar.name}`],
    [ColorModeButton, ColorModeButton.name, `<${ColorModeButton.name}></${ColorModeButton.name}`],
    [ColorModeImage, ColorModeImage.name, `<${ColorModeImage.name}></${ColorModeImage.name}`],
    [ColorModeSelect, ColorModeSelect.name, `<${ColorModeSelect.name}></${ColorModeSelect.name}`],
    [ColorModeToggle, ColorModeToggle.name, `<${ColorModeToggle.name}></${ColorModeToggle.name}`],
    [ContentSearch, ContentSearch.name, `<${ContentSearch.name}></${ContentSearch.name}`],
    [ContentSearchButton, ContentSearchButton.name, `<${ContentSearchButton.name}></${ContentSearchButton.name}`],
    [ContentSurround, ContentSurround.name, `<${ContentSurround.name}></${ContentSurround.name}`],
    [ContentToc, ContentToc.name, `<${ContentToc.name}></${ContentToc.name}`],
    [DashboardCard, DashboardCard.name, `<${DashboardCard.name}></${DashboardCard.name}`],
    [DashboardLayout, DashboardLayout.name, `<${DashboardLayout.name}></${DashboardLayout.name}`],
    [DashboardModal, DashboardModal.name, `<${DashboardModal.name}></${DashboardModal.name}`],
    [DashboardNavbar, DashboardNavbar.name, `<${DashboardNavbar.name}></${DashboardNavbar.name}`],
    [DashboardNavbarToggle, DashboardNavbarToggle.name, `<${DashboardNavbarToggle.name}></${DashboardNavbarToggle.name}`],
    [DashboardPage, DashboardPage.name, `<${DashboardPage.name}></${DashboardPage.name}`],
    [DashboardPanel, DashboardPanel.name, `<${DashboardPanel.name}></${DashboardPanel.name}`],
    [DashboardPanelContent, DashboardPanelContent.name, `<${DashboardPanelContent.name}></${DashboardPanelContent.name}`],
    [DashboardPanelHandle, DashboardPanelHandle.name, `<${DashboardPanelHandle.name}></${DashboardPanelHandle.name}`],
    [DashboardSearch, DashboardSearch.name, `<${DashboardSearch.name}></${DashboardSearch.name}`],
    [DashboardSearchButton, DashboardSearchButton.name, `<${DashboardSearchButton.name}></${DashboardSearchButton.name}`],
    [DashboardSection, DashboardSection.name, `<${DashboardSection.name}></${DashboardSection.name}`],
    [DashboardSidebar, DashboardSidebar.name, `<${DashboardSidebar.name}></${DashboardSidebar.name}`],
    [DashboardSidebarLinks, DashboardSidebarLinks.name, `<${DashboardSidebarLinks.name}></${DashboardSidebarLinks.name}`],
    [DashboardSlideover, DashboardSlideover.name, `<${DashboardSlideover.name}></${DashboardSlideover.name}`],
    [DashboardToolbar, DashboardToolbar.name, `<${DashboardToolbar.name}></${DashboardToolbar.name}`],
    [Field, Field.name, `<${Field.name}></${Field.name}`],
    [FieldGroup, FieldGroup.name, `<${FieldGroup.name}></${FieldGroup.name}`],
    [Footer, Footer.name, `<${Footer.name}></${Footer.name}`],
    [FooterColumns, FooterColumns.name, `<${FooterColumns.name}></${FooterColumns.name}`],
    [FooterLinks, FooterLinks.name, `<${FooterLinks.name}></${FooterLinks.name}`],
    [Header, Header.name, `<${Header.name}></${Header.name}`],
    [HeaderLinks, HeaderLinks.name, `<${HeaderLinks.name}></${HeaderLinks.name}`],
    [HeaderPopoverLinks, HeaderPopoverLinks.name, `<${HeaderPopoverLinks.name}></${HeaderPopoverLinks.name}`],
    [LandingCTA, LandingCTA.name, `<${LandingCTA.name}></${LandingCTA.name}`],
    [LandingCard, LandingCard.name, `<${LandingCard.name}></${LandingCard.name}`],
    [LandingFAQ, LandingFAQ.name, `<${LandingFAQ.name}></${LandingFAQ.name}`],
    [LandingGrid, LandingGrid.name, `<${LandingGrid.name}></${LandingGrid.name}`],
    [LandingHero, LandingHero.name, `<${LandingHero.name}></${LandingHero.name}`],
    [LandingLogos, LandingLogos.name, `<${LandingLogos.name}></${LandingLogos.name}`],
    [LandingSection, LandingSection.name, `<${LandingSection.name}></${LandingSection.name}`],
    [LandingTestimonial, LandingTestimonial.name, `<${LandingTestimonial.name}></${LandingTestimonial.name}`],
    [Main, Main.name, `<${Main.name}></${Main.name}`],
    [NavigationAccordion, NavigationAccordion.name, `<${NavigationAccordion.name}></${NavigationAccordion.name}`],
    [NavigationLinks, NavigationLinks.name, `<${NavigationLinks.name}></${NavigationLinks.name}`],
    [NavigationTree, NavigationTree.name, `<${NavigationTree.name}></${NavigationTree.name}`],
    [Page, Page.name, `<${Page.name}></${Page.name}`],
    [PageBody, PageBody.name, `<${PageBody.name}></${PageBody.name}`],
    [PageCard, PageCard.name, `<${PageCard.name}></${PageCard.name}`],
    [PageColumns, PageColumns.name, `<${PageColumns.name}></${PageColumns.name}`],
    [PageError, PageError.name, `<${PageError.name}></${PageError.name}`],
    [PageGrid, PageGrid.name, `<${PageGrid.name}></${PageGrid.name}`],
    [PageHeader, PageHeader.name, `<${PageHeader.name}></${PageHeader.name}`],
    [PageHero, PageHero.name, `<${PageHero.name}></${PageHero.name}`],
    [PageLinks, PageLinks.name, `<${PageLinks.name}></${PageLinks.name}`],
    [PricingCard, PricingCard.name, `<${PricingCard.name}></${PricingCard.name}`],
    [PricingGrid, PricingGrid.name, `<${PricingGrid.name}></${PricingGrid.name}`],
    [PricingToggle, PricingToggle.name, `<${PricingToggle.name}></${PricingToggle.name}`],
    [Shortcut, Shortcut.name, `<${Shortcut.name}></${Shortcut.name}`],
    [Tabs, Tabs.name, `<${Tabs.name}></${Tabs.name}`],
  ]
}
